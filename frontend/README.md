# SHARE frontend

**[Full documentation → repository root README](../../../README.md)**

Next.js 16 App Router UI. Local quick start from this directory:

```bash
npm install
npm run dev
```

Backend should run at `http://127.0.0.1:8000` (see `lib/axios.ts`).
